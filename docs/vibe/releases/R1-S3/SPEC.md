# R1-S3 需求：数据与 D1/D2 可证伪先导

状态：首个工程切片 FROZEN，2026-09-24（用户当日明确授权）；完整研究数据准入与 D1/D2 效果仍受 G1–G3 门禁约束。这不是论文题目或三研究内容冻结。学术依据见[第二轮裁决](../../../../thesis/D1_D2第二轮查新与立题裁决_20260924.md)，工程基线见 [S2 验证](../R1-S2/VERIFICATION.md)。用户已授权直接在当前本地目录改工程，保留现有未提交修改；不建隔离工作树。

## 目标与非目标

目标：把 S2 的 D1 检索工程接到**可审计真实样本**，完成离线证据层的同预算可证伪先导；同时为高风险 D2 建立固定候选的 guard 覆盖挑战清单和可运行的简单基线。首期不承诺 D1/D2 方法已成立，不运行全面收费实验，不接完整多智能体、Foundry、Mythril 深度求解或全链 fork。

## 验收需求

| ID | 需求 | 验收证据 |
|---|---|---|
| S3-01 | 来源登记与准入：保留来源 URL/版本/许可/源码哈希、漏洞位置/类别、报告与补丁谱系、审查状态；无法核实标待审，不自动当真值 | 可重放来源 manifest 与拒收原因；原始数据只读 |
| S3-02 | 按项目、事件、补丁、规范化源码及近克隆成组；知识/开发/验证/锁定测试隔离，文本、报告、模板继承组 ID | 分组图和泄漏报告；跨划分任何确认连边即失败 |
| S3-03 | 审核员独立标案例—目标适用性、支持/反证价值、保护事实与不确定；相同模型的自评不能作标签 | 带证据引用、版本与审查者记录的标签；争议保持未决 |
| S3-04 | 在同一候选池和**整条下游提示** token 上限下比较 Dense、Hybrid、普通正反、简单条件字段过滤、当前 D1；冻结模型相关模板版本 | 检索层每样本预算、候选、排序、选中证据可重放；无真实模型时明确仅检索评估 |
| S3-05 | D1 消融至少含无目标绑定、无互补选择；指标含适用性 Recall@K/nDCG、误入选率、有效对照覆盖、分条件错误分析 | 项目为独立统计单位，输出逐样本及聚合；零分母/失败/unknown 不美化 |
| S3-06 | D2 仅做固定真假假设 challenge 与 guard 存在、具体代码行、路径过滤等基线契约；无法获得可靠真假标签就只记录问题集 | 固定假设、标签出处与错误接受/拒绝/unknown 分母；不宣称 D2 方法有效 |
| S3-07 | 付费模型试验必须单独 dry-run，明确端点、deepseek-v4.1-flash、最大请求数/token/重试与可能费用；无显式运行选择不调用 | 调用计划与回执；usage 缺失保留 null，失败不可转安全 |
| S3-08 | 负结果也要产出保留/收窄/暂停建议；任何方法差异必须与简单强基线比较 | 有决定依据的研究门禁记录，不以测试通过替代研究效果 |

## 数据准入顺序

优先调查 SCRUBD 的重入真实案例、SmartBugs Curated 的隔离知识、ASE 2025 访问控制基准与 ACFix 的真实漏洞/补丁；来源可用性、公开许可、版本和负例质量必须逐项核验。SolidiFI 与旧名义安全合约仅用于 legacy 回归/故障测试，不作为正式外部真实性结果。真实补丁对、人工最小差异变体、合成夹具分开统计。首次先导样本规模按可靠独立项目数决定，不为达成 24 对目标引入伪标签。

## 研究门禁

G1：完成来源、标签与组隔离审计后，才准锁定开发先导。G2：离线 D1 与强基线比较，若仅字段过滤即可解释收益，D1 降级。G3：D2 只有固定真假候选和可审查保护路径证据时才进入方法原型；若既有过滤器达到相同双向错误/成本结果则暂停。G4：锁定测试只有在方法、阈值、tokenizer、提示和预算冻结后打开。不得因为临近开题日期绕过 G1–G3。

验证仍以离线测试为默认：根 Maven clean verify、Python unittest；真实 API/Milvus/工具须显式配置。先导实验与工程单元测试分别汇报。
